[plugin]
model_tier = expensive

# How often (in seconds) to edit the status message with transcription progress
# after the first update. The first update fires within ~30s of segments appearing.
update_interval = 60
