services:
  discord-bot:
    build: .
    container_name: discord-chatbot
    restart: unless-stopped
    environment:
      - TZ=${TIMEZONE:-Europe/Berlin}
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
